import { PagePlaceholder } from '#/components/page-placeholder';

export function RuntimesPage() {
  return (
    <PagePlaceholder
      title="Runtimes"
      description="Local agent runtime management will be added here."
    />
  );
}
