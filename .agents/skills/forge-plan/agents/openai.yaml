interface:
  display_name: "Forge Plan"
  short_description: "Shape goals into reviewable plans and tasks"
  default_prompt: "Use $forge-plan to turn my goal into a reviewable plan and connected task sequence."
policy:
  allow_implicit_invocation: false
