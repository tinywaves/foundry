import { PagePlaceholder } from '#/components/page-placeholder';

export function McpsPage() {
  return (
    <PagePlaceholder
      title="MCPs"
      description="MCP server management will be added here."
    />
  );
}
